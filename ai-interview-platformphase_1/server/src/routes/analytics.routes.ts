import { Router } from "express";

const router = Router();

// GET /overview, GET /score-trend, GET /category-breakdown
// Implemented in Phase 6 alongside the history and progress charts.

export default router;
