# Services

Business logic layer, called by controllers. `auth.service.ts` / `user.service.ts`
land in Phase 1, `interview.service.ts` in Phase 4/5, `analytics.service.ts` in Phase 6.
See `ai/` for the AI provider abstraction (Phase 4).
