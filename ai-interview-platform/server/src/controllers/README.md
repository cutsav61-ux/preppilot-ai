# Controllers

Thin, HTTP-only request handlers. Business logic lives in `services/`, not here.
`auth.controller.ts` and `user.controller.ts` land in Phase 1, `interview.controller.ts`
in Phase 4/5, `analytics.controller.ts` in Phase 6.
