import { Router } from "express";

const router = Router();

// POST /, GET /, GET /:id, POST /:id/answers, POST /:id/complete,
// PATCH /:id/abandon, DELETE /:id
// Implemented in Phase 4 (generator) and Phase 5 (live flow + evaluation).

export default router;
