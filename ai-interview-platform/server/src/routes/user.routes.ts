import { Router } from "express";

const router = Router();

// GET/PUT /profile, PUT /password, PUT /settings, DELETE /account
// Implemented in Phase 1 (auth-dependent) and Phase 3 (profile/settings UI).

export default router;
