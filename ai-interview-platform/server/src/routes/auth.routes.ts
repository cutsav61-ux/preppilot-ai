import { Router } from "express";

const router = Router();

// POST /signup, POST /login, POST /refresh, POST /logout, GET /me
// Implemented in Phase 1 alongside the User model and auth middleware.

export default router;
